# Placeholder - Express server runs via frontend
import os
import subprocess
import time

# Keep this process alive but do nothing
# The real server runs from /app via npm run dev
while True:
    time.sleep(3600)
